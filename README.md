# ai agentic repo
clone the repo at first
then create your .env file, and if you want also move to an environment virtual venv
install the hugging face hub for using the model directly from hugging face (optionnal) with [pip install -q huggingface_hub]
get your huggingface token at huggingface/tokens and paste it on your .env
